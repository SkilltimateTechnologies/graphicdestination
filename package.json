{
  "name": "graphicdestination-motion",
  "version": "1.1.0",
  "private": true,
  "description": "GraphicDestination Motion — browser-native motion graphics studio (SaaS)",
  "scripts": {
    "install:all": "cd client && npm ci && cd ../server && npm ci",
    "build": "cd client && npm ci && npm run build && cd ../server && npm ci",
    "start": "node server/index.js",
    "dev:server": "cd server && npm start",
    "dev:client": "cd client && npm run dev",
    "test": "cd server && node test-api.mjs"
  },
  "engines": { "node": ">=20" }
}
